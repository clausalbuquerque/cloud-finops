# Azure Consumption Extractor

Tool to extract, store and correlate the Azure consumption data using the Azure Cost Management API.

## Structure

- `src/` – Source code
- `tests/` – Unit and integration tests
- `config/` – Configuration files (API keys, endpoints, etc.)
- `docs/` – Documentation
