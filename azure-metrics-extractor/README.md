# Azure Metrics Extractor

Tool to extract usage metrics for Azure resources (CPU, memory, network, etc.).

## Structure

- `src/` – Source code
- `tests/` – Unit and integration tests
- `config/` – Configuration files (API keys, endpoints, etc.)
- `docs/` – Documentation
