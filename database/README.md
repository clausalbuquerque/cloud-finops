# Cloud FinOps Database

Database to hold all cloud consumption and metrics data.

## Structure

- `migrations/` – Database migration scripts
- `schemas/` – Schema definitions
- `scripts/` – Utility scripts (seed data, maintenance, etc.)
- `docs/` – Documentation
